#include<stdio.h>
int main()
{
   int n=-5;

   if(n>0)
        printf("It is positive no.");
   else if(n<0)
        printf("It is negative no.");
   else
        printf("Number is zero");

    return 0;
}

